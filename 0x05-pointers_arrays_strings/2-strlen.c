#include "main.h"
#include <string.h>
/**
 * _strlen - function that returns the length of a string
 * @len: length of  a string
 * @str: string variable
 * Return: length of a string
 */
int _strlen(char *s)
{
	int count = 0;

	while (s[count] != '\0')
	[
		count++;
	]
	return (count);

}
